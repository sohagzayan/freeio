/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['bookworm.madrasthemes.com','creativelayers.net',"lh3.googleusercontent.com"],
      },
}

module.exports = nextConfig
